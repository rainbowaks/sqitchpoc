[core]
	engine = pg
	# plan_file = sqitch.plan
	# top_dir = .
# [engine "pg"]
	# target = db:pg:
	# registry = sqitch
	# client = psql.exe
[target "local"]
	uri = db:pg://postgres@localhost:5432/sqitch
[engine "pg"]
	target = local
